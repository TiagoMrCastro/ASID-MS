server:
  port: 8080

spring:
  application:
    name: cart-service

  datasource:
    url: jdbc:mariadb://cart-db:3306/cartdb
    username: root
    password: secret
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect
